# Seeds for crates/yadorilink-daemon/tests/monkey_chaos.rs that
# previously produced a failing run, one per line. Automatically appended
# to by that test on failure (see `record_failing_seed`); always replayed
# by the `replay_known_failing_seeds` test in the same file, in CI and in
# scripts/heat-run.sh, regardless of whether the underlying bug has since
# been fixed -- once fixed, the entry becomes a permanent regression
# check rather than being deleted.
#
# Lines starting with '#' and blank lines are ignored.
1254137095109609298
13961652510686304188
12788561176311549428
